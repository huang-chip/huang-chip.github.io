name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
    paths:
      - 'drtao-vue-app/**'  # 只有Vue项目代码变化时才触发
  pull_request:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: 'drtao-vue-app/package-lock.json'
        
    - name: Install dependencies
      run: |
        cd drtao-vue-app
        npm ci
        
    - name: Build
      run: |
        cd drtao-vue-app
        npm run build
      env:
        VITE_LLM_API_URL: ${{ secrets.VITE_LLM_API_URL }}
        VITE_LLM_API_KEY: ${{ secrets.VITE_LLM_API_KEY }}
        VITE_LLM_DEFAULT_MODEL: ${{ secrets.VITE_LLM_DEFAULT_MODEL }}
        
    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      if: github.ref == 'refs/heads/main'
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./drtao-vue-app/dist
        cname: huang-chip.github.io